# DESARROLLO LOCAL (sin Docker)
# Esta configuración es para cuando ejecutas el backend directamente con npm run dev
# El backend normalmente corre en el puerto 5000 en desarrollo local
VITE_API_URL=http://localhost:5000/api
